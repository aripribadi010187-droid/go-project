apiVersion: v1
kind: Service
metadata:
  name: employee-backend
spec:
  selector:
    app: employee-backend
  ports:
    - name: http
      port: 8888
      targetPort: 8888
  type: ClusterIP